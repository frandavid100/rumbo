name: Catalog Mercadona 39191 precision revalidation

on:
  push:
    branches: [agent/catalog-phase1]
    paths:
      - '.github/workflows/catalog-mercadona-39191-precision-revalidation.yml'
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: catalog-mercadona-39191-precision-revalidation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  revalidate:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      GH_TOKEN: ${{ github.token }}
      PADDLE_PDX_MODEL_SOURCE: BOS
      PRODUCT_ID: '39191'
      ANCHOR_EAN: '8480000391919'
      ANCHOR_IMAGE_URL: 'https://prod-mercadona.imgix.net/images/e87da104435f6de38f059b3831563fbb.jpg?fit=crop&h=3600&w=3600'
      PRIOR_PILOT_RUN_ID: '35169416349'
      PRIOR_PILOT_ARTIFACT_ID: '10476537659'
      PRIOR_PILOT_SHA256: '36c2c34a56ad256fb1f729e0d13133e2ec1da7b9473bc0eb4fdce8336afe250e'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Verify precision regression exists on checked-out SHA
        working-directory: tools/catalog
        shell: bash
        run: |
          set -euo pipefail
          grep -F 'ENSEMBLE_VERSION = "1.3.3"' nutrition_ocr_ensemble.py
          python -m unittest -v \
            test_nutrition_ocr_ensemble.OCREnsembleTest.test_exact_cross_family_consensus_beats_higher_confidence_nearby_layout_value

      - name: Rehydrate official first-party detail and require exact historical identity/image
        working-directory: tools/catalog
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json, os
          from pathlib import Path
          from mercadona_first_party_details import _get_json, normalize, _now

          pid = os.environ['PRODUCT_ID']
          anchor_ean = os.environ['ANCHOR_EAN']
          anchor_url = os.environ['ANCHOR_IMAGE_URL']
          observed_at = _now()
          payload, source_url = _get_json(pid, timeout=20.0)
          current = normalize(payload, source_url=source_url, observed_at=observed_at)
          current_ean = str(current.get('ean') or '').strip()
          if current_ean != anchor_ean:
              raise SystemExit(f'Current EAN mismatch for {pid}: current={current_ean!r} anchor={anchor_ean!r}')
          photos = current.get('photos') if isinstance(current.get('photos'), list) else []
          matches = [
              (index, photo)
              for index, photo in enumerate(photos)
              if isinstance(photo, dict)
              and str(photo.get('perspective') or '') == '9'
              and str(photo.get('zoom') or '') == anchor_url
          ]
          if len(matches) != 1:
              raise SystemExit(f'Exact historical first-party perspective=9 image is not uniquely current for {pid}: {len(matches)} matches')
          image_index, photo = matches[0]
          routed = dict(photo)
          routed['perspective'] = 9
          current['photos'] = [routed]
          current['_precision_revalidation_meta'] = {
              'canonical_ean': anchor_ean,
              'current_ean': current_ean,
              'image_url': anchor_url,
              'image_index': image_index,
              'prior_pilot_run_id': int(os.environ['PRIOR_PILOT_RUN_ID']),
              'prior_pilot_artifact_id': int(os.environ['PRIOR_PILOT_ARTIFACT_ID']),
              'prior_pilot_sha256': os.environ['PRIOR_PILOT_SHA256'],
              'live_detail_source_url': source_url,
              'live_detail_observed_at': observed_at,
          }
          Path('precision-revalidation-product.jsonl').write_text(
              json.dumps(current, ensure_ascii=False, sort_keys=True) + '\n', encoding='utf-8'
          )
          print(json.dumps({
              'product_id': pid,
              'ean_exact': True,
              'image_exact_unique_p9': True,
              'image_index': image_index,
              'source_url': source_url,
              'images_persisted': False,
              'redistribution_allowed': False,
          }, sort_keys=True))
          PY

      - name: Install OCR dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends tesseract-ocr tesseract-ocr-spa
          python -m pip install --disable-pip-version-check paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
          python -m pip install --disable-pip-version-check \
            'paddleocr>=3.7,<3.8' \
            'easyocr>=1.7,<1.8' \
            'python-doctr==1.1.0' \
            opencv-python-headless Pillow

      - name: Run conservative regressions before OCR
        working-directory: tools/catalog
        run: |
          python -m unittest -v \
            test_mercadona_ambiguous_primary_column_rescue.py \
            test_mercadona_bounded_doctr_rescue.py \
            test_mercadona_near_safe_doctr_retry.py \
            test_label_doctr_extractor.py \
            test_mercadona_ocr_safety_regressions.py \
            test_nutrition_label_reader.py \
            test_nutrition_label_reader_ocr_typos.py \
            test_nutrition_label_reader_row_tokens.py \
            test_nutrition_ocr_ensemble.py \
            test_label_image_preprocess.py

      - name: Re-run exact-image primary-column OCR under ensemble 1.3.3
        working-directory: tools/catalog
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p mercadona-39191-precision-revalidation/raw
          mode=$(python - <<'PY'
          import json
          from pathlib import Path
          row = json.loads(Path('precision-revalidation-product.jsonl').read_text(encoding='utf-8'))
          print('priority' if row.get('ingredients') else 'p9-no-ingredients-all')
          PY
          )
          set +e
          python mercadona_ambiguous_primary_column_rescue.py \
            --products precision-revalidation-product.jsonl \
            --out mercadona-39191-precision-revalidation/raw \
            --shard-index 0 \
            --shard-count 1 \
            --limit 0 \
            --eligibility-mode "$mode"
          rc=$?
          set -e
          [[ $rc -eq 0 || $rc -eq 2 ]]

      - name: Validate corrected fresh observation and materialize audit bundle
        working-directory: tools/catalog
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import glob, json, os, sqlite3
          from pathlib import Path

          CORE = ('calories', 'fat_g', 'carbohydrate_g', 'protein_g')
          root = Path('mercadona-39191-precision-revalidation')
          current = json.loads(Path('precision-revalidation-product.jsonl').read_text(encoding='utf-8'))
          meta = current.get('_precision_revalidation_meta') or {}
          rows = []
          for path in sorted(glob.glob(str(root / 'raw/results-*.jsonl'))):
              rows.extend(json.loads(line) for line in Path(path).read_text(encoding='utf-8').splitlines() if line.strip())
          if len(rows) != 1 or str(rows[0].get('product_id') or '') != os.environ['PRODUCT_ID']:
              raise SystemExit(f'Unexpected result coverage: {[(row.get("product_id"), row.get("status")) for row in rows]}')
          row = rows[0]
          if str(row.get('ean') or '') != os.environ['ANCHOR_EAN']:
              raise SystemExit('OCR result EAN does not match exact historical/current anchor')
          if str(row.get('image_url') or '') != os.environ['ANCHOR_IMAGE_URL']:
              raise SystemExit('OCR used an unexpected image')
          if row.get('status') != 'DECLARED':
              raise SystemExit(f'Fresh precision revalidation did not remain DECLARED: {row.get("status")}')
          if row.get('basis') != '100_ml':
              raise SystemExit(f'Unexpected basis: {row.get("basis")}')
          nutrition = row.get('nutrition') or {}
          if any(nutrition.get(field) is None for field in CORE):
              raise SystemExit('Fresh DECLARED result is missing a core macro')
          expected = {'calories': 50.0, 'fat_g': 0.0, 'carbohydrate_g': 11.3, 'protein_g': 0.5}
          if nutrition != expected:
              raise SystemExit(f'Fresh exact-consensus nutrition mismatch: got={nutrition!r}, expected={expected!r}')
          declared_attempts = [a for a in row.get('attempts', []) if (a.get('ensemble') or {}).get('status') == 'DECLARED']
          if len(declared_attempts) < 1:
              raise SystemExit('No independently DECLARED fresh attempt')
          ensemble = declared_attempts[0].get('ensemble') or {}
          if (
              ensemble.get('basis') != '100_ml'
              or int(ensemble.get('independent_engine_families') or 0) < 2
              or int(ensemble.get('corroborated_fields') or 0) < 4
              or ensemble.get('reasons')
              or (ensemble.get('nutrition') or {}) != expected
          ):
              raise SystemExit('Fresh attempt does not independently meet unchanged DECLARED contract with corrected exact consensus')
          if (
              row.get('source') != 'MERCADONA_FIRST_PARTY'
              or row.get('source_record_kind') != 'label image'
              or row.get('evidence_level') != 'OCR_DERIVED_FROM_MERCADONA_IMAGE'
              or row.get('redistribution_allowed') is not False
          ):
              raise SystemExit('Provenance contract changed unexpectedly')

          row['image_index'] = meta.get('image_index')
          row['perspective'] = 9
          row['live_detail_source_url'] = meta.get('live_detail_source_url')
          row['live_detail_observed_at'] = meta.get('live_detail_observed_at')
          row['precision_revalidation'] = {
              'prior_pilot_run_id': meta.get('prior_pilot_run_id'),
              'prior_pilot_artifact_id': meta.get('prior_pilot_artifact_id'),
              'prior_pilot_sha256': meta.get('prior_pilot_sha256'),
              'old_stored_carbohydrate_g': 11.39,
              'corrected_carbohydrate_g': 11.3,
              'ensemble_version': '1.3.3',
              'acceptance_policy_changed': False,
              'historical_values_promotion_allowed': False,
              'cross_run_value_fusion': False,
              'cross_crop_value_fusion': False,
          }
          out = root / 'results-merged.jsonl'
          out.write_text(json.dumps(row, ensure_ascii=False, sort_keys=True) + '\n', encoding='utf-8')
          summary = {
              'inventory_products': 4280,
              'processed': 1,
              'product_id': os.environ['PRODUCT_ID'],
              'status': 'DECLARED',
              'nutrition': expected,
              'basis': '100_ml',
              'corroborated_fields': int(ensemble.get('corroborated_fields') or 0),
              'independent_engine_families': int(ensemble.get('independent_engine_families') or 0),
              'precision_correction': {'carbohydrate_g_before': 11.39, 'carbohydrate_g_after': 11.3},
              'evidence_level': 'OCR_DERIVED_FROM_MERCADONA_IMAGE',
              'source': 'MERCADONA_FIRST_PARTY/label image',
              'redistribution_allowed': False,
              'images_persisted': False,
              'missing_values_inferred': False,
              'structured_api_macros_claimed': False,
              'acceptance_policy_changed': False,
              'historical_values_promotion_allowed': False,
              'cross_run_value_fusion': False,
              'cross_crop_value_fusion': False,
              'CLASSIFIED': 0,
              'MENU_ELIGIBLE': 0,
          }
          (root / 'summary.json').write_text(json.dumps(summary, ensure_ascii=False, indent=2, sort_keys=True) + '\n', encoding='utf-8')
          db = sqlite3.connect(root / 'staging.sqlite')
          db.execute('CREATE TABLE IF NOT EXISTS ocr_rows (product_id TEXT PRIMARY KEY, ean TEXT, status TEXT, basis TEXT, calories REAL, fat_g REAL, carbohydrate_g REAL, protein_g REAL, evidence_level TEXT, source TEXT, source_record_kind TEXT, image_url TEXT, redistribution_allowed INTEGER, raw_json TEXT NOT NULL)')
          db.execute('INSERT OR REPLACE INTO ocr_rows VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?)', (
              row.get('product_id'), row.get('ean'), row.get('status'), row.get('basis'),
              expected['calories'], expected['fat_g'], expected['carbohydrate_g'], expected['protein_g'],
              row.get('evidence_level'), row.get('source'), row.get('source_record_kind'), row.get('image_url'), 0,
              json.dumps(row, ensure_ascii=False, sort_keys=True),
          ))
          db.commit(); db.close()
          print(json.dumps(summary, sort_keys=True))
          PY

      - name: Upload auditable precision revalidation
        uses: actions/upload-artifact@v4
        with:
          name: mercadona-39191-precision-revalidation
          path: |
            tools/catalog/mercadona-39191-precision-revalidation/results-merged.jsonl
            tools/catalog/mercadona-39191-precision-revalidation/summary.json
            tools/catalog/mercadona-39191-precision-revalidation/staging.sqlite
            tools/catalog/mercadona-39191-precision-revalidation/raw/*.jsonl
            tools/catalog/mercadona-39191-precision-revalidation/raw/*.json
          if-no-files-found: error
          retention-days: 30

      - name: Trigger strict residual reconciliation
        shell: bash
        run: |
          set -euo pipefail
          gh workflow run catalog-mercadona-current-ocr-residual-audit.yml --ref agent/catalog-phase1
