name: Apply dynamic surface tuning

on:
  push:
    branches: [agent/certified-day-witness-level1]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/certified-day-witness-level1
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Aplicar ajuste visual
        run: |
          python tools/tune_dynamic_surfaces_and_search.py
          git diff --check
      - name: Guardar cambios
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/es/david/rumbo/ui/Theme.kt app/src/main/java/es/david/rumbo/ui/App.kt
          git commit -m "Afinar fondo tarjetas y superficie de búsqueda"
          git pull --rebase origin agent/certified-day-witness-level1
          git push origin HEAD:agent/certified-day-witness-level1
