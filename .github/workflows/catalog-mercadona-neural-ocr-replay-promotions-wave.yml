name: Catalog Mercadona neural OCR replay promotions wave

on:
  push:
    branches: [agent/catalog-phase1]
    paths:
      - '.github/workflows/catalog-mercadona-neural-ocr-replay-promotions-wave.yml'
  workflow_dispatch:

permissions:
  actions: read
  contents: read

concurrency:
  group: catalog-mercadona-neural-ocr-replay-promotions-wave-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      PADDLE_PDX_MODEL_SOURCE: BOS
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/catalog-phase1

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Download consolidated Mercadona first-party cut
        uses: actions/download-artifact@v4
        with:
          name: mercadona-first-party-final
          path: tools/catalog/mercadona-first-party-final
          github-token: ${{ github.token }}
          run-id: 33046950149

      - name: Download current persisted-text replay
        uses: actions/download-artifact@v4
        with:
          name: mercadona-ocr-replay-2630
          path: tools/catalog/mercadona-ocr-replay-2630-current
          github-token: ${{ github.token }}
          run-id: 33308586301

      - name: Install independent OCR engines and detector dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends tesseract-ocr tesseract-ocr-spa
          python -m pip install --disable-pip-version-check paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
          python -m pip install --disable-pip-version-check 'paddleocr>=3.7,<3.8' 'easyocr>=1.7,<1.8' opencv-python-headless Pillow

      - name: Run parser and ensemble safety regressions first
        working-directory: tools/catalog
        run: |
          python -m unittest -v \
            test_nutrition_label_reader.py \
            test_nutrition_label_reader_ocr_typos.py \
            test_nutrition_ocr_ensemble.py \
            test_mercadona_label_pipeline.py \
            test_mercadona_ocr_safety_regressions.py

      - name: Select remaining highest-confidence independently corroborated untrusted replay promotions
        working-directory: tools/catalog
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          trusted_manifest = json.loads(Path('mercadona-p9-trusted-id-manifest-2026-08-30.json').read_text(encoding='utf-8'))
          trusted = set(map(str, trusted_manifest['original_declared_product_ids']))
          trusted.update(map(str, trusted_manifest.get('independently_audited_replay_promotions', [])))
          trusted.update(map(str, trusted_manifest.get('independently_reproduced_typo_recovery_promotions', [])))

          replay_rows = []
          for line in Path('mercadona-ocr-replay-2630-current/results-replayed.jsonl').read_text(encoding='utf-8').splitlines():
              if not line.strip():
                  continue
              row = json.loads(line)
              pid = str(row.get('product_id') or '')
              replay = row.get('replay') or {}
              if pid in trusted or replay.get('prior_status') != 'REVIEW' or replay.get('status') != 'DECLARED':
                  continue
              declared = [a for a in replay.get('attempt_ensembles', []) if a.get('status') == 'DECLARED']
              if not declared:
                  continue
              best = max(declared, key=lambda a: (a.get('independent_engine_families', 0), a.get('confidence', 0.0)))
              if best.get('independent_engine_families', 0) < 3 or best.get('corroborated_fields', 0) != 4:
                  continue
              nutrition = best.get('nutrition') or {}
              required = ('calories', 'protein_g', 'carbohydrate_g', 'fat_g')
              if any(nutrition.get(field) is None for field in required):
                  continue
              replay_rows.append({
                  'product_id': pid,
                  'name': row.get('name'),
                  'basis': best.get('basis'),
                  'nutrition': nutrition,
                  'confidence': float(best.get('confidence') or 0.0),
                  'independent_engine_families': int(best.get('independent_engine_families') or 0),
                  'corroborated_fields': int(best.get('corroborated_fields') or 0),
              })

          replay_rows.sort(key=lambda r: (-r['confidence'], r['product_id']))
          selected = replay_rows[:16]
          if not selected:
              raise SystemExit('No eligible high-confidence replay promotions remain')

          wanted = {row['product_id'] for row in selected}
          inventory = [
              json.loads(line)
              for line in Path('mercadona-first-party-final/products.jsonl').read_text(encoding='utf-8').splitlines()
              if line.strip()
          ]
          products = [row for row in inventory if str(row.get('product_id') or '') in wanted]
          found = {str(row.get('product_id') or '') for row in products}
          if found != wanted:
              raise SystemExit(f'Missing product ids from first-party cut: {sorted(wanted - found)}')

          Path('mercadona-replay-promotion-wave-selection.json').write_text(
              json.dumps({
                  'kind': 'BOUNDED_HIGH_CONFIDENCE_REPLAY_PROMOTION_E2E_SELECTION',
                  'selection_count': len(selected),
                  'trusted_count_before': len(trusted),
                  'selection': selected,
              }, ensure_ascii=False, indent=2, sort_keys=True),
              encoding='utf-8',
          )
          Path('mercadona-replay-promotion-wave-products.jsonl').write_text(
              ''.join(json.dumps(row, ensure_ascii=False, sort_keys=True) + '\n' for row in products),
              encoding='utf-8',
          )
          print(json.dumps(selected, ensure_ascii=False, indent=2))
          PY

      - name: Re-run neural OCR end to end on bounded wave
        working-directory: tools/catalog
        run: |
          python mercadona_neural_ocr_wave.py \
            --products mercadona-replay-promotion-wave-products.jsonl \
            --out mercadona-neural-ocr-replay-promotions-wave \
            --shard-index 0 \
            --shard-count 1 \
            --limit 0

      - name: Verify exact replay agreement, corroboration and provenance
        working-directory: tools/catalog
        run: |
          python - <<'PY'
          from collections import Counter
          import json
          from pathlib import Path

          selection_doc = json.loads(Path('mercadona-replay-promotion-wave-selection.json').read_text(encoding='utf-8'))
          expected = {row['product_id']: row for row in selection_doc['selection']}
          rows = []
          out = Path('mercadona-neural-ocr-replay-promotions-wave')
          for path in sorted(out.glob('results-*.jsonl')):
              rows.extend(json.loads(line) for line in path.read_text(encoding='utf-8').splitlines() if line.strip())

          actual_ids = {str(row.get('product_id') or '') for row in rows}
          if actual_ids != set(expected):
              raise SystemExit(f'Unexpected processed universe: {sorted(actual_ids)}')

          reproduced = []
          review = []
          conflicts = []
          unsafe = []
          provenance_errors = []
          nutrition = {}
          for row in rows:
              pid = str(row.get('product_id') or '')
              if row.get('evidence_level') != 'OCR_DERIVED_FROM_MERCADONA_IMAGE':
                  provenance_errors.append(f'{pid}:evidence_level')
              if row.get('source') != 'MERCADONA_FIRST_PARTY' or row.get('source_record_kind') != 'label image':
                  provenance_errors.append(f'{pid}:source')
              if row.get('redistribution_allowed') is not False:
                  provenance_errors.append(f'{pid}:redistribution')
              if any(key in row for key in ('classified', 'menu_eligible', 'CLASSIFIED', 'MENU_ELIGIBLE')):
                  provenance_errors.append(f'{pid}:classification')

              if row.get('status') != 'DECLARED':
                  review.append(pid)
                  continue

              accepted = next((a for a in (row.get('attempts') or []) if (a.get('ensemble') or {}).get('status') == 'DECLARED'), None)
              ensemble = (accepted or {}).get('ensemble') or {}
              observed = row.get('nutrition') or {}
              required = ('calories', 'protein_g', 'carbohydrate_g', 'fat_g')
              if ensemble.get('independent_engine_families', 0) < 2 or ensemble.get('corroborated_fields', 0) != 4:
                  unsafe.append(pid)
                  continue
              if any(observed.get(field) is None for field in required):
                  unsafe.append(pid)
                  continue
              replay_nutrition = expected[pid]['nutrition']
              if any(float(observed[field]) != float(replay_nutrition[field]) for field in required):
                  conflicts.append({'product_id': pid, 'replay': replay_nutrition, 'end_to_end': observed})
                  continue
              if row.get('basis') != expected[pid]['basis']:
                  conflicts.append({'product_id': pid, 'replay_basis': expected[pid]['basis'], 'end_to_end_basis': row.get('basis')})
                  continue
              reproduced.append(pid)
              nutrition[pid] = observed

          if provenance_errors:
              raise SystemExit('Provenance contract violated: ' + ','.join(provenance_errors))
          if unsafe:
              raise SystemExit('Unsafe DECLARED rows: ' + ','.join(unsafe))
          if conflicts:
              raise SystemExit('Replay conflicts: ' + json.dumps(conflicts, ensure_ascii=False))

          summary = {
              'mode': 'BOUNDED_E2E_VALIDATION_OF_HIGH_CONFIDENCE_REPLAY_PROMOTIONS',
              'processed': len(rows),
              'status_counts': dict(sorted(Counter(row.get('status', 'UNKNOWN') for row in rows).items())),
              'selected_ids': sorted(expected),
              'reproduced_ids': sorted(reproduced),
              'review_ids': sorted(review),
              'declared_nutrition': nutrition,
              'conflicts': conflicts,
              'unsafe_declared': unsafe,
              'provenance_errors': provenance_errors,
              'safe_for_trusted_addition': sorted(reproduced),
              'canonical_additions_applied': 0,
              'evidence_level': 'OCR_DERIVED_FROM_MERCADONA_IMAGE',
              'source': 'MERCADONA_FIRST_PARTY/label image',
              'redistribution_allowed': False,
              'images_persisted': False,
              'missing_values_inferred': False,
              'CLASSIFIED': 0,
              'MENU_ELIGIBLE': 0,
          }
          Path('mercadona-replay-promotions-wave-e2e-summary.json').write_text(
              json.dumps(summary, ensure_ascii=False, indent=2, sort_keys=True),
              encoding='utf-8',
          )
          print(json.dumps(summary, ensure_ascii=False, indent=2, sort_keys=True))
          PY

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mercadona-neural-ocr-replay-promotions-wave
          path: |
            tools/catalog/mercadona-neural-ocr-replay-promotions-wave/
            tools/catalog/mercadona-replay-promotion-wave-selection.json
            tools/catalog/mercadona-replay-promotions-wave-e2e-summary.json
          retention-days: 30
          if-no-files-found: warn
